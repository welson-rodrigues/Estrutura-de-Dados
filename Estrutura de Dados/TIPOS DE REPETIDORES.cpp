#include <iostream>
using namespace std;
int main()
{
    int vetor[10] = {73, 67, 56, 32, 52, 41, 83, 37, 32, 10}
    int tamanho_vetor = sizeof(vetor) / sizeof(vetor[0]); // tamanho do vetor
    int gap = tamanho_vetor / 2; // o gap seria o tamanho do vetor dividido por 2
    for(gap )
    
    return 0;
}
